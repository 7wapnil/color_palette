<p><%= link_to "Edit color", edit_color_path(color), class: "btn btn-primary btn-xs" %> <%= link_to "Delete color", color_path(color), class: "btn btn-danger btn-xs", method: :delete, data: { confirm: "Sure to delete color permanently?" } %></p>