<p><%= link_to "Edit palette", edit_palette_path(palette), class: "btn btn-primary btn-xs" %> <%= link_to "Delete palette", palette_path(palette), class: "btn btn-danger btn-xs", method: :delete, data: { confirm: "Sure to delete palette permanently?" } %></p>